#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>
int main(void)
{
	int t;
	scanf("%d", &t);
	while(t--)
	{
		int n, str[55];
		scanf("%d", &n);
		scanf("%s", str);
		int len = strlen(str);
	}
	return 0;
}